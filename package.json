{
  "name": "giftsshop",
  "version": "1.0.0",
  "description": "Built Ecommerce Website similar to amazon",
  "main": "index.js",
  "scripts": {
    "deploy": "./deploy.sh",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "cd backend && npm install && npm run build  && cd ../frontend && npm install && npm run build",
    "start": "cross-env TG_ALLOW_MIXED=ALLOW node backend/build/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Giftmasil/final-personal-project.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/basir/mern-amazona/issues"
  },
  "homepage": "https://github.com/basir/mern-amazona#readme",
  "devDependencies": {
    "concurrently": "^8.0.1",
    "cross-env": "^7.0.3"
  }
}
